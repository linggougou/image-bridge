# Security

## 信任边界

image-bridge 在用户本机运行，连接三方：

1. **回环桥**（默认 `127.0.0.1:47831`）：只监听回环地址，用 bearer token 鉴权，
   仅接受来自扩展来源（`chrome-extension://`）与本地 CLI 的请求
2. **Chrome 扩展**：不读 Cookie、不读浏览器密码或历史；只与专用 ChatGPT 标签页通信
3. **ChatGPT 网页**：提示词与参考图会提交给该服务

token 存放于 `~/.image-bridge/extension-bridge-token`（权限 `0600`），扩展侧存于 `chrome.storage.local`。
两者都不应出现在仓库、日志或工单里。

## 明确不做的事

- 不自动登录、不代填凭据、不保存账号密码
- 不绕过人机验证、CAPTCHA、区域或浏览器安全检查；遇到时任务失败并交回人工
- 不调用未公开接口或私有 API
- 不导出 Cookie 或会话存储

## 使用者的责任

image-bridge 自动化的是**用户已手动登录的网页会话**，不是官方 API。
OpenAI 个人版条款限制自动或程序化提取输出，因此使用本项目可能不符合适用条款，
并存在账号受限或功能随页面改版失效的风险。使用者须自行确认授权与合规性；
正式生产、批量或对外服务请改用官方 Image API。

## 报告问题

请通过本仓库的 GitHub Issues 报告安全问题；不要在公开 issue 中粘贴 token、
会话信息、提示词内容或参考图。若必须提供诊断信息，请确认其中不含凭据与个人数据。
